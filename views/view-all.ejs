<!doctype html>
<html lang=en>

<head>
	<title>View Bio Requests</title>
	<link rel="icon" href="/favicon.png">
	<link rel="stylesheet" href="/style.css">
</head>

<body>
	<%- include("header") -%>
	<main id="content">
		<p>Please note that due to the fact that this uses the Store npm package, your request might get deleted.</p>
		<div id="requests">
			<% for (let d = 0; d <= Object.keys(data).length; d++) { %>
			<div class="changeRequest">
				<span><%- Object.keys(data)[d] -%></span>
				<span><%- data[Object.keys(data)[d]] -%></span>
			</div>
			<% } %>
		</div>
	</main>
	<%- include("footer") -%>
</body>

</html>